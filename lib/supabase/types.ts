/**
 * Placeholder for generated Supabase types.
 * TODO: replace with `supabase gen types typescript --project-id ioitmtstrryvjkypaexq`
 * output, wired into a package.json script (`npm run gen:types`) and CI.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
